#include <bits/stdc++.h>
using namespace std;
int main() {
    // vector<int> v;

    // for(int i=1;i<=16; i++)
    // {
    //     v.push_back(i);
    //     cout<<v.back()<<"  "<<"Size : "<<v.size()<<"   ::  Capacity : "<<v.capacity()<<endl;
    // }

    // vector <int> Vector1(10);    //default value 0 
    // vector <int> Vector2(10,1);    //default value 1 
    // vector <int> Vector3(10,1222);    //default value 12 for each element
    // for(int i=0; i<Vector1.size(); i++)
    //     cout<<Vector1[i]<<" "<<Vector2[i]<<" "<<Vector3[i]<<endl;

    // 1   .push_back(val);
    // 2   .size();
    // 3   .capacity();
    // 4   .pop_back();
    // 5   .resize();
    //      to check empty or not .empty() if empty 1 else 0
    //      .clear() ->> to clear vector 
    //      to sort  -->>    sort(nums.begin(), nums.end());


    
    return 0;
}
